// Backward-compatible export for older imports. All execution is server-side through Judge0.
export { Judge0Executor, languageIds } from '../code/executor.js';
export type { Executor, ExecutionResult, ExecutionStatus } from '../code/executor.js';
